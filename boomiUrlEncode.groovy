import java.net.URLEncoder

// Input URL containing special characters
String inputUrl = inputValue;

// Perform full URL encoding
String encodedUrl = URLEncoder.encode(inputUrl, "UTF-8");

// Print the encoded URL
encodedValue = encodedUrl;